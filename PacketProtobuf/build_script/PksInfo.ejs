#pragma once

<% for (var file of headerList) { %>
#include "<%= file %>.h"
<% } %>

// Declaration only
template <class PKS>
class PKSInfo;
<% for (var i in list) {
    var pksName = list[i].pksName;
    var enumType = list[i].enumType;
    var enumName = list[i].enumName;
%>
template <>
class PKSInfo<<%= pksName %>>
{
public:
	static const <%= enumType %> enumVal = <%= enumName %>;
};
<% } %>