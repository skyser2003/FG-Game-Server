#pragma once

<% for (var file of headerList) { %>
#include "<%= file %>.h"
<% } %>

// Declaration only
template <int ENUM>
class PKENUMInfo;
<% for (var i in list) {
    var pksName = list[i].pksName;
    var enumType = list[i].enumType;
    var enumName = list[i].enumName;
%>
template <>
class PKENUMInfo<static_cast<int>(<%= enumName %>)>
{
public:
	using PKS = <%= pksName %>;
};
<% } %>